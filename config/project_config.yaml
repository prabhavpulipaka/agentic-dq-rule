project:
  name: agentic-dq-rule-author

databricks:
  catalog: workspace
  schema: dq_demo

dataset:
  seed: 42

  row_counts:
    customers: 10000
    orders: 25000
    order_items: 60000
    payments: 25000

  date_range:
    start: "2024-01-01"
    end: "2025-12-31"

  write_mode: overwrite