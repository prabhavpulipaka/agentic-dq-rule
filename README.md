# Agentic DQ Rule Author

Tools for generating clean data, injecting controlled defects, and profiling tables for data-quality rule authoring.

## Development

```bash
python -m pip install -e ".[dev]"
pytest
```
